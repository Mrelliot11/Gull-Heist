# For a VPS: Caddy gets the HTTPS certificate automatically and
# forwards everything (including the /ws WebSocket) to the game.
# Run the game with TRUST_PROXY=1 so per-IP limits see real client addresses.
yourgame.gg {
	reverse_proxy localhost:8080
}
