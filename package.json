{
  "name": "gull-heist",
  "version": "1.0.0",
  "description": "Gull Heist: a top-down seagull food-heist game with online multiplayer",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "cap:sync": "npx cap sync android",
    "android:build": "npx cap sync android && cd android && gradlew.bat assembleDebug",
    "android:build:unix": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2"
  }
}
